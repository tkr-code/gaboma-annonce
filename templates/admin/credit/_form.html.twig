{{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="btn-group">
        <a class="btn btn-dark" href="{{ path('admin_credit_index') }}">back to list</a>
        <button class="btn btn-{{ app.request.attributes.get('_route') == 'admin_credit_new' ? 'primary':'success' }} ">{{ button_label|default('Save') }}</button>
    </div>
{{ form_end(form) }}
